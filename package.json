{
  "name": "drywall",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "test": "node --import ./test/setup.js --test \"test/**/*.test.js\"",
    "test:integration": "node --import ./test/setup.js --test \"test/**/*.integration.js\"",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "prettier": "^3.8.1"
  }
}
